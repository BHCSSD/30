# 30.2.11 ASSIGN Merge Algorithm
```
30.2.11 ASSIGN Merge Algorithm
```

### Scenario: Interstellar Data Synchronization

You are a systems technician aboard the USS Enterprise. Your task is to synchronize two separate data streams for the new Starfleet Database: the **Klingon Starfleet Registry** (containing ship IDs and their battle readyness) and the **Vulcan Science Directorate Registry** (containing research probes and data logs obviously).

Both registries maintain their data streams in a state of **perfect alphabetical order** (by ship/probe designation). You must use the **interweaving merge** technique to combine them into a single, chronological master log without triggering a full resort.

-----

### Step-by-Step Instructions

### 1. Project Setup

  * Create a new Python file named: `StarfleetMerge.py`
  * All code must be organized within functions.
  * Follow all style guides

### 2. Create and Sort Registries

  * Create an unsorted list called `Klingon_Registry` with 8 unique ship/battle cruiser designations (e.g. `"IKS Vorn"`, `"B'rel Bird-of-Prey"`).

      * Each item must be a unique string designation.

  * Sort this list alphabetically using one of our sorting algos
  * Create an unsorted second list called `Vulcan_Registry` with 8 unique probe/research vessel designations (e.g. `"V'tosh Ka'tur"`, `"T'Plana-Hath"`).
    * Sort this list alphabetically as well

### 3. Display Initial Data

Print both lists before merging to verify the initial state:

```python
print("Klingon Starfleet Registry (Sorted Alpha):")
print("\nVulcan Science Directorate Registry (Sorted Alpha):")
```

### 4. Define the `merge()` Function

Define a function called `merge(list1, list2, logic_merge)` that handles both simple concatenation (illogical, brute-force) and the complex interweaving merge (logical, efficient).

```python
def merge(list1, list2, logic_merge):
    """
    Merges two Starfleet data streams.
    If logic_merge is True (Vulcan logic), it performs an interweaving merge (optimized for sorted lists).
    If logic_merge is False (Klingon brute force), it performs a simple concatenation.

    Parameters:
        list1 (list): The first registry list.
        list2 (list): The second registry list.
        logic_merge (bool): Flag indicating merge type.

    Returns:
        list: The final synchronized master log.
    """
    mergedList = []
    # Implementation Guide details go here
    return mergedList
```

-----

> [!TIP]
> 
> **Python List Methods Reminder**
>
> | Protocol | Purpose | Example | Result |
> | :--- | :--- | :--- | :--- |
> | `.append(item)` | Adds a **single, discrete entity** to the log. | `a = [1, 2]; a.append(3)` | `[1, 2, 3]` |
> | `.extend(iterable)` | Adds **all elements** from a list individually (used for bulk transfer). | `a = [1, 2]; a.extend([3, 4])` | `[1, 2, 3, 4]` |
>
> **Quick Rule:** Use `.append()` for single-item processing (in the comparison loop). Use `.extend()` for transferring leftover data streams.


-----

### 5. Execute and Print Results

Ask your users which form of merg they would like to use. Keep the theme going. 

