# **30.2.04 ASSIGN: Binary Search Everyday Superhero**

```python
30.2.04 ASSIGN Binary Search Everyday Superhero

```

In this assignment, you will build a superhero hiring database. You are encouraged to find a clever or funny way to demonstrate binary searching (e.g. themed hero names or snarky error messages).

### **Standard Requirements**

* **Testing:** You must create your own `assert` statements for deep testing within a dedicated `test_functions()` (or `testFunction()`).
* **Documentation:** Each function must include a **docstring** describing its purpose, parameters, and return values.
* **Robustness:** Handle all user errors (text instead of numbers, out-of-range values) using `try/except` blocks.
* **Code Grammar:** Follow all Unit 4 rules (f-strings, capital `L` for lists, no global logic).

---

## **Part 1: Set Up and Parallel Lists (40%)**

Create a binary search function and build a superhero hiring database using parallel lists.

### **Requirements**

1. **Greeting:** Print "Welcome to SuperHeroes for Hire!"
2. **Parallel Lists:** Inside `main()`, create three parallel lists of at least **ten** superheroes:
* `name`: Superhero names (e.g. "Mighty Mouse").
* `power`: Power levels (**floats** from 1.00 to 9.99). **Must be in ascending order.**
* `price`: Hourly rates (up to $300/hr).


3. **Display:** Print "Available Power Levels:" followed by the sorted `power` list so the user knows what is available.

> [!NOTE]
> 
> **Parallel Lists Reminder**
> 
> If `binarySearch` returns index `1`, that index links all three lists: `name[1]`, `power[1]`, and `price[1]`.

---

## **Part 2: Searching Using Binary Search (40%)**

Accurately match the user's required power level to an available hero using Binary Search.

### **Steps**

1. **Input Validation:** Define `get_user_power()` to prompt the user for a level (1.00-9.99).
    * Use `try/except ValueError` to catch **non-numeric** input.
    * Use a loop to re-prompt until a valid float within the range is entered.
       * If you want to be extra, [check here](https://www.geeksforgeeks.org/python/python-find-closest-number-to-k-in-given-list/) to find the closest num in a list

2. **Search:** Use your `binarySearch()` function to find the index of the power level.
3. **Output:**
    * **Found:** Print the hero's name and hourly rate using f-strings.
    * **Not Found:** Print a message stating no hero matches that exact strength.

---

## **Part 3: Hours and Total Cost (10%)**

### **The "Alberta Rules" Calculation**

If a hero is found, ask for the number of hours needed. Define `calculate_total_cost(rate, hours)` based on these rules:

* **Minimum Pay:** Alberta law requires a minimum of **3 hours** pay. If the user enters 1 or 2, they are billed for 3. Use `max(3, hours)`.
* **Flat Fee:** Add a **20% flat fee** (tax/insurance) to the total.

**Calculation Example:**

* Rate: $130 | Hours: 2
* Billed Hours: `max(3, 2)` = 3
* Base: $390 | Total: $390 * 1.20 = **$468.00**

---

## **Part 4: Labor Laws and Exit Loop (10%)**

### **Alberta Labor Regulations (Break Schedule)**

Define `display_labor_law(hours)` to inform the user of required breaks based on the following table:

| Hours Worked | Law / Break Required |
| --- | --- |
| under 5 | No breaks required |
| 5 | One 30-minute break |
| 10+ | Two 30-minute breaks |
| 12+  | **Not allowed** (Maximum 12 hours/day) |

### **The Loop**

* Continue asking for power levels until the user enters **0 for hours**.
* When `0` is entered, print an exit message (e.g. "Fine. Maybe you don’t deserve superheroes.") and terminate the program.

---

## **Required Functions Template**

```python
def binarySearch(L, target):
    """Performs binary search on sorted list L. Returns index or -1."""
    pass

def get_user_power():
    """Validates and returns a float between 1.00 and 9.99."""
    pass

def calculate_total_cost(rate, hours):
    """Calculates cost with 3-hour minimum and 20% tax."""
    pass

def display_labor_law(hours):
    """Prints break requirements based on hours."""
    pass

def test_functions():
    """Contains all assert statements for search and cost calculations."""
    # Test binarySearch: first, last, middle, missing
    # Test calculate_total_cost: 2 hours (min pay), 5 hours, etc.
    print("All tests passed!")

def main():
    """Main program loop."""
    pass

if __name__ == "__main__":
    test_functions()
    main()

```

---

## **Deep Testing Checklist**

* [ ] **Binary Search:** Tested first element, last element, and a value that doesn't exist.
* [ ] **Cost Calculation:** Verified that hiring for 1 hour results in 3 hours of pay + 20% tax.
* [ ] **Labor Laws:** Verified that exactly 5 hours shows "No breaks" while 5.1 shows "One 30-minute break."
* [ ] **Validation:** Program does not crash if I type "POTATO" as the power level.
* [ ] **Loop:** Program exits smoothly only when `0` is entered for hours.

**Submit to D2L:** `30.2.04 ASSIGN Binary Search Everyday Superhero`
